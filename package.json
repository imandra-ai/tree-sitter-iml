{
  "name": "tree-sitter-iml",
  "version": "0.27.0",
  "description": "IML (Imandra Modeling Language) grammar for tree-sitter",
  "repository": "https://github.com/imandra-ai/tree-sitter-iml",
  "license": "MIT",
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "iml",
    "ocaml"
  ],
  "files": [
    "binding.gyp",
    "bindings/node/*",
    "common/**",
    "grammars/iml/src/**",
    "prebuilds/**",
    "queries/*",
    "*.wasm"
  ],
  "dependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "eslint-config-treesitter": "^1.0.2",
    "prebuildify": "^6.0.1",
    "tree-sitter-cli": "^0.25.5"
  },
  "peerDependencies": {
    "tree-sitter": "^0.22.4"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "lint": "eslint grammars/*/grammar.js",
    "generate": "for dir in grammars/iml; do cd $dir; tree-sitter generate; cd -; done",
    "prestart": "tree-sitter build grammars/iml --wasm -o grammars/iml/tree-sitter-iml.wasm",
    "start": "tree-sitter playground --grammar-path grammars/iml",
    "test": "node --test bindings/node/*_test.js",
    "parse-examples": "test/parse-examples.sh"
  }
}
